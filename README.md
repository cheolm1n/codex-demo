# Game Catalog

Vue 3 + Express example application for browsing games.

## Development

Install dependencies and run the server:

```bash
npm install
npm run dev
```

Access the app at `http://localhost:3000`.

## Build

```bash
npm run build
node server.js
```
